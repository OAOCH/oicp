node_modules/
dist/
data/*.db
data/*.db-wal
data/*.db-shm
.env
*.log
.sync-token
.sync-cursor.json
.sync-pending-finalize

# Estado del rellenado (cursor local, como .sync-cursor.json)
.sync-repair-cursor.json
rellenado.log

# Volcados masivos del SERCOP (temporales, ~150 MB por anio)
.volcado-*.zip

# Cursor del indice del SOCE (estado local)
.soce-cursor.json
masivo*.log
